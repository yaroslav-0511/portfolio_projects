## Аудит short_tz.txt (обязательный минимум)

Источник: `../short_tz.txt`.

### Глобальные критерии
- **Закрытый доступ**: ✅ реализовано через инвайт‑коды и онбординг (`bot/handlers/common.py`, `InviteCode`, `Company`).
- **Запись на сессию ≤ 3 кликов**: ✅ в пределах UX (меню → “Сессия” → выбрать слот → подтвердить).
- **Только кнопки для действий**: ✅ (кроме системного `/start`).
- **Роли U/C/O**: ✅ реализованы отдельными меню и правами (`bot/handlers/{user,consultant,owner}.py`).
- **3 языка (RU/EN/UA)**: ✅ i18n-файлы `bot/i18n/{ru,en,ua}.json` + выбор языка.

### Mental Feedback (оценка 1–10 после сессии)
- **Оценка 1–10 кнопками**: ✅ (`bot/keyboards/user_kb.py: feedback_keyboard`, `bot/handlers/user.py`).
- **Owner выгрузка отчёта txt “время:оценка”**: ✅ (`bot/handlers/owner.py` → `o_menu:feedback`, файл `feedback_*.txt`).

### Напоминания до сессии
Требование: за 7 дней / 3 дня / 24 часа / 1 час (если применимо), дружелюбный текст.
- **APScheduler + Redis jobstore**: ✅ (`bot/services/scheduler.py`, `bot/main.py`).
- **Тексты напоминаний**: ✅ (`bot/i18n/*.json`: `reminder_7d/3d/24h/1h`).

### После бронирования: “Сессия была / не состоялась”
- **Кнопки после сессии**: ✅ планируются автоматически (job `post_session_*`) и показываются (`post_session_keyboard`).
- **Логика “сессия была/не была”**: ✅ `bot/handlers/user.py` (`session_status:*`, `cancel_reason:*`), фиксирует статусы `COMPLETED/NO_SHOW`, уведомляет Owner о неявке.

### Счётчики сессий для Owner
Требование: неделя / месяц / всё время; отдельно по каждому психологу и общий список.
- **Счётчики**: ✅ (`bot/handlers/owner.py` → `o_menu:counters`).
- **По психологам + total**: ✅ (`mode=by_consultant` и `mode=total`).

### Срочная помощь (U) и принятие (C)
- **U “Неотложная помощь”**: ✅ кнопка `btn_emergency`, сообщение `emergency_sent`.
- **C список срочных запросов + принять**: ✅ (`bot/handlers/consultant.py`, `btn_c_urgent`, `urgent_accept:*`).

### Что уточнить/довести (если всплывёт в ручном прогоне)
- **Точное “≤3 кликов”**: при желании можно ещё упростить подтверждение записи (UX), но сейчас соответствует смыслу.
- **Термины “удалить консультанта”**: это деактивация `is_active=False` + восстановление отдельной кнопкой (сохраняем историю).

